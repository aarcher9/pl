#include <stdio.h>
#include <stdlib.h>

typedef int rows;
typedef int columns;
typedef double* items;

typedef struct {
        rows n_of_rows;
        columns n_of_columns;
        items elements;
} Matrix;

// Matrix new_matrix(int n_of_rows, int n_of_columns) {
//         Matrix m;

//         m.n_of_rows = n_of_rows;
//         m.n_of_columns = n_of_columns;
//         m.elements = (double *) malloc((n_of_rows *n_of_columns) * sizeof(int));
// };

int main() {

        return 0;
}