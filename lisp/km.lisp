;; Operazioni fra vettori

;; *
(defun vplus (x y) 
        (print "somma fra vettori"))

;; *
(defun vminus (x y) 
        (print "differenza fra vettori"))

;; *
(defun scalarprod (x y) 
        (print "prodotto per uno scalare"))

;; *
(defun innerprod (x y) 
        (print "prodotto interno"))

;; *
(defun norm (x y) 
        (print "prodotto interno"))


;; Algoritmo k-means

;; *
(defun kmeans (observations k) 
        (print "kmeans"))

;; *
(defun centroid (observations) 
        (print "centroid"))
