#include "SubMoney.hpp"
#include <string>
#include <math.h>
using namespace std;

SubMoney::SubMoney(float val, char curr) : Money(val, curr) {

    // Non accessibile (private)
    // this->value;

    
};